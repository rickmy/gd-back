export class CreateCareerDto {
  code: string;
  codeAuth: string;
  resolutionNumber: string;
  name: string;
  titleAwarded: string;
  state: boolean;
  instituteId: string;
  modalityId: string;
  typeCareerId: string;
}
