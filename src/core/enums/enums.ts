export enum TypeDNI {
    CEDULA,
    RUC,
    PASAPORTE,
  }
  
  export enum StatusCompany {
    PENDIENTE,
    APROBADA,
    RECHAZADA,
    RETIRADA,
    ACTIVO,
  }
  
  export enum StatusStudent {
    ASIGNADO,
    RETIRADO,
    PENDIENTE,
    ACTIVO,
    INACTIVO,
    PERDIDO,
  }
  
  export enum StatusProject {
    PENDIENTE,
    ACTIVO,
    INACTIVO,
  }
  